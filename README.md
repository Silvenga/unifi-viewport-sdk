# unifi-viewport-sdk

This project is an attempt to reverse-engineer the device adoption protocol for the Ubiquiti Protect Viewport
appliance (When did Unifi Protect get dropped from marketing?) and a resulting Rust SDK for this protocol.
